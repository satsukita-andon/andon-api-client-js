import * as auth from './auth';
import * as articles from './articles';
import * as users from './users';
import * as festivals from './festivals';
import { ASC, DESC } from './common';

export default {
  auth,
  articles,
  users,
  festivals,
  ASC,
  DESC
}
